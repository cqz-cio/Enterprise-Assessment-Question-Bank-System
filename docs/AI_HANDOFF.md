# 新开发会话交接说明

> 这是新 Codex/AI 开发会话的首要入口。请先阅读，不要仅凭原项目 README 开始修改。

## 1. 项目目标

本项目基于 yf-boot-exam 二次开发为“企业人才考核系统”，固定支持 `INTERVIEW`（面试）、`REGULARIZATION`（转正）、`PROMOTION`（晋升）三个场景。

最重要的业务区别：

- 部门继续使用组织架构；岗位由管理员维护并可关联多个部门，岗位下可配置职级。
- HR 不创建岗位或组卷规则，只录入候选人、按部门选择岗位并发放已有面试考核；候选人字段为姓名、候选人编号、手机号、邮箱、部门、岗位、批次、开始时间和截止时间。
- 候选人不注册，通过固定 `/exam-entry` 页面使用姓名 + 6 位考核码进入。
- 员工自由注册，注册后由系统管理员或 HR 审核。
- 员工注册的手机号和邮箱均为选填。
- 系统管理员和 HR 可为员工发放已有考核。
- 候选人考核默认有效期为 14 天，HR 可在发放前调整开始和截止时间。
- 每条考核分配只允许一次作答；未交卷恢复原试卷，交卷或过期后禁止重考。
- 候选人和员工最终只查看是否通过，不显示分数、答案和解析；客观题结算或主观题最终阅卷后自动公布。
- 客观题自动评分；简答题人工阅卷。

## 2. 开始任务前必须阅读

按顺序读取：

1. `docs/README.md`
2. `docs/01-product-requirements.md`
3. `docs/02-system-design.md`
4. 当前任务涉及数据库时读取 `docs/03-data-model.md`
5. 当前任务涉及接口时读取 `docs/04-api-spec.md`
6. `docs/05-development-plan.md`
7. `docs/08-decision-log.md`

如果实现与文档冲突，先指出冲突并更新决策，不要默默选择原项目行为。

## 3. 当前仓库与技术栈

- 工作区：`D:\Enterprise Assessment Question Bank System`
- 后端：`yf-bev2-api`
- 前端：`yf-bev2-vue`
- 初始化 SQL：`yf_boot_exam.sql`
- Docker Compose：`compose.yaml`
- 后端技术：Java 17、Spring Boot 3.2.1、Shiro、JWT、MyBatis-Plus、MySQL、Redis、Quartz。
- 前端技术：Vue 3、TypeScript、Element Plus、Vite。
- 后端端口：8080。
- 数据库/Redis 容器：`yf-exam-mysql`、`yf-exam-redis`。
- 原始导入 tag：`baseline-yf-boot-exam`。

用户已经确认当前项目能够运行。不要未经检查重新导入数据库或删除数据卷。

## 4. 当前工作区注意事项

在本文档建立时，工作区存在以下未提交内容：

```text
M  yf-bev2-api/src/main/resources/application-dev.yml
?? compose.yaml
?? work/
?? yf-bev2-api/logs/
?? docs/
```

这些内容可能包含用户或前序会话的工作。任何修改前必须执行：

```powershell
git status --short
git diff -- yf-bev2-api/src/main/resources/application-dev.yml
```

不得使用 `git reset --hard`、`git checkout --` 或删除未知文件来清理工作区。

## 5. 已验证的现有能力

- 题库、题目、选项基础 CRUD。
- 单选、多选、判断题。
- 按题型、数量、分值随机抽题。
- 每个用户进入考试时创建自己的试卷。
- 答题实时保存。
- 限时和 Quartz 自动交卷。
- 客观题完全匹配自动评分。
- 原有考试次数、最低交卷时间、迟到限制；其中“考试次数”不能直接用于新业务，V1 考核分配固定不允许重考。
- 用户、部门、角色、菜单权限。
- 员工式账号注册开关和注册审核开关。
- 成绩按姓名、分数和通过状态基础筛选。

## 6. 已发现但尚未修复的重要问题

1. Phase 1 已修复空密码绕过、JWT 验签、密钥外部化和 Redis 当前 token 绑定；运行前必须配置至少 32 字符的 `JWT_SECRET`。
2. Phase 1 已为试卷详情、答题、交卷和实时状态接口增加统一 owner 校验；Phase 2 已继续接入分配状态、截止时间和唯一试卷校验。
3. 已提供 `HR`、`EMPLOYEE`、`CANDIDATE` 角色、员工审核和 Phase 2 业务迁移；`V001`～`V005` 位于后端标准 Flyway 目录，由应用启动自动执行，禁止手工重复执行。
4. 员工注册前后端已实现工号/部门必填、手机/邮箱选填以及注册后等待审核。
5. 岗位 CRUD 已扩展为部门多选和岗位职级维护；题库/模板按部门、岗位、面试/转正/晋升场景及可选晋升职级配置；候选人分配、6 位考核码和 `/exam-entry` 已实现。
6. `scripts/dev/import-initial-position-structure.sql` 可重复导入截图中已确认的餐具部、家具部及开发业务员、增长业务员；职级未确认，因此不预设。
7. V008 将可见组织树固定为“全品轩 → 餐具部、家具部”；旧演示部门停用隐藏但不物理删除，以保护历史关联。
6. 试卷题目和选项仍关联题库当前内容，不是真正历史快照。
7. 选项顺序没有随机。
8. 简答题题库录入、Excel 导入及参考答案/评分要点存储已实现；答题、人工阅卷和最终结算闭环尚未实现。
9. 题目 Excel 模板下载、预校验、部分成功导入、重复跳过和错误报告已实现；成绩导出仍无成品接口。
10. 当前 `application-dev.yml` 日志级别为 debug，不适合生产。
11. 当前 Compose 将 MySQL/Redis 端口映射到宿主机，只适合本地开发，不是最终生产配置。

## 7. 推荐下一项开发任务

先启动应用确认 Flyway 成功应用并验收 `V001`～`V005`，完成 Phase 2 浏览器联调；随后进入 Phase 3：不可变试卷快照、题目启停和选项随机。Phase 0 的环境基线整理仍需并行补齐。

## 8. 开发实现约束

- 不大规模重命名原包、表和类。
- 数据库变化必须新增版本化 SQL，不只修改 `yf_boot_exam.sql`。
- 不将明文考核码、真实 token、密码或生产密钥写入日志和 Git。
- 候选人仍映射内部 `el_sys_user`，复用现有 JWT/Redis/Shiro 和 `user_id` 关联。
- 候选人考核码验证成功后签发普通 JWT，而不是建立第二套并行会话系统。
- 所有答题接口必须以后端当前身份为准，不接受客户端指定 userId。
- 创建试卷和交卷必须幂等。
- 一条考核分配在整个生命周期内只能关联一张试卷，不允许通过旧版考试次数配置开启重考。
- 考生侧交卷和结果接口不得返回分数、标准答案或解析，最终仅返回是否通过。
- Excel 导入采用部分成功策略：正确行落库，错误行返回逐行报告。
- 新试卷必须保存不可变题目和选项快照。
- 保留用户已有未提交修改；只修改当前任务必要文件。

## 9. 验证要求

- 后端测试/构建必须使用有限超时，不能让命令无限阻塞。
- 开发服务器应以可管理的后台进程运行，并记录 PID 和日志路径。
- 至少验证正常路径、未登录、权限不足、横向越权、重复请求和过期状态。
- UI 改动需要在实际浏览器中验证关键页面，不只依赖编译通过。
- 每次结束时报告修改文件、验证结果、未解决问题和下一步。

## 10. 可直接复制给新会话的提示词

```text
请先完整阅读本仓库 docs/AI_HANDOFF.md，并按其中顺序阅读相关工程文档。
然后执行 git status，检查并保护现有未提交修改。

本次任务：<在这里填写 Phase 和具体任务>。

实现必须遵守 docs/01-product-requirements.md、docs/03-data-model.md、
docs/04-api-spec.md 和 docs/05-development-plan.md。先核对现有代码，再实施、测试并更新文档；
不要扩大任务范围，不要清理或覆盖与本次任务无关的用户文件。
```

## 11. 会话结束交接模板

```markdown
### 本次目标

### 已修改文件

### 数据库迁移

### 新增或变更接口

### 验证结果

### 未完成和风险

### 下一会话建议任务
```
