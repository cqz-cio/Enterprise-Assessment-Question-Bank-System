# Linux 测试环境交付记录（2026-09-21）

## 状态

服务器 `124.220.2.69` 的独立测试环境已完成安装、完整数据恢复和服务器内部功能验收。公网 `18443` HTTPS 仍待腾讯云防火墙放行及外部复测，**当前不能宣称已完成外部上线验收**。

拟用入口 `https://124.220.2.69:18443/#/login`，候选人入口 `/#/exam-entry`。原 IP 80 端口已有 ERP，保留其根路由；原域名 443 站点保持不变。Nginx 80 仅新增 ACME 验证路径，测试站使用独立 18443。

## 数据与身份

用户明确要求完整复制本机数据，又明确选择独立测试密钥、新建考核任务验收。因此保留题库、账号密码、候选人、历史试卷及成绩，但不复制原 JWT_SECRET/ASSIGNMENT_CODE_PEPPER、不复制 Redis 会话。旧考核码在测试站不能认证；本机原码和密钥不变。密钥只在服务器受限配置文件中保存。

初始备份 `work/backups/ops/20260921-093459-ff6cb42a`，已核对 SQL/附件 SHA256，恢复到空的专用库，40 张表、685 行、V024、3 个附件与清单一致。仅调整测试库本地附件插件路径为 `/opt/enterprise-exam-test/uploads/`。

## 发布与部署变更

- 先将既有品牌清理及 V022/V023 等纳入提交 `ae46d5c08e5f70ec13a826b9b261e2c63d005a1b`，使实际发布源码可追溯。
- 发布包 `yf-bev2-api/target/releases/20260921-094422-27645b9b`：115 项后端测试通过，正式前端构建通过，137 个 JAR 前端文件匹配。清单 dirty 来自当时未跟踪部署文件等，应用源码已提交。
- 新增 `deploy/linux/`：独立 MySQL/Redis Compose、systemd 应用服务、私有配置示例、独立滚动日志、Nginx HTTPS/ACME 配置、证书续期服务/定时器、Linux 私有备份及操作说明。
- 使用服务器现有 Java 17 的绝对路径，未改变全局 Java 8 默认版本。运行用户 exam-test，仅 uploads/logs 可写。
- 后端 127.0.0.1:18082，MySQL 127.0.0.1:13307，Redis 127.0.0.1:16380。各自专用凭据和资源限制，不使用原 ERP 数据库或 Redis。
- 首次启动发现底座日志配置写当前目录、Redis 降权后无法读取 0600 配置，分别以独立日志配置和受控组读权限修复；随后应用 NRestarts=0，稳定运行。
- IP HTTPS 证书已签发，独立六小时续期定时器已启用；无业务数据定时备份或异机复制计划。

## 验证

- Flyway 启动校验成功，数据库版本 V024，无新迁移，无业务 API 变更。
- 真实 HTTP/MySQL 验证：验证码生成与临时管理员登录、未登录拒绝、旧模板开考入口拒绝、四并发开考仅一卷、跨用户读写/交卷拒绝、候选人管理接口拒绝、快照不受源题修改影响、四并发交卷一次结算、只返回通过结果、禁止重考和交卷后改答、到期自动结算、停用拒绝、提交后原有效期内以新测试码查结果。
- 验收使用新建虚拟岗位、题库、账号和候选人，结束清理临时数据、会话、防重记录、试卷和 Quartz 任务；八张原核心表计数一致。日志 `work/codex-logs/20260921-100045-linux-acceptance.log`。
- 服务器内部 HTTPS 证书信任验证成功，首页 200、`/doc.html` 和 `/v3/api-docs` 为 404、未认证受保护 API 拒绝。伪造 X-Forwarded-For 被 Nginx 覆盖，后端限流记录真实来源。
- 独立证书续期服务实际运行成功，Certbot `renew --dry-run --no-random-sleep-on-renew` 模拟续期通过；六小时 systemd timer 已启用。
- 实际备份 `/opt/enterprise-exam-test/backups/ops/20260921-100317-c95a5f0e` 完成；数据库、附件及独立运行密钥的清单哈希和 0600 权限复查通过。该备份包含明文测试运行密钥，整体只允许受限访问，异机传输须加密。
- 服务器验收后原 ERP 根页面 HTTP 200，原两个 ERP 容器保持运行。应用内存约 392 MB，服务器可用内存约 1 GB，仅适合少量人员联调；未执行 100 人压力测试。

## 待完成

1. 用户在腾讯云轻量服务器防火墙放行入站 TCP 18443 后，复测公网 HTTPS、前端资源和实际浏览器登录/候选人入口。
2. 暂未做本次服务器管理端浏览器完整业务验收；API 验收不能代替真实浏览器全流程。
3. 业务数据备份调度、加密异机副本及灾备恢复需下一步落实；当前已提供并实际运行手工私有备份。
4. 前端既有 32 项类型问题仍保留。正式多人使用前需容量测试、运行观察及运维负责人确认。

详细启动、备份、升级、证书和权限说明见 [Linux 部署说明](../deploy/linux/README.md)。本机原数据、原密钥及已有未跟踪 AGENTS.md/patches 均保留。
