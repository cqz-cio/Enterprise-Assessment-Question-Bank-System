# Include inside the existing IP server on port 80; leaves its application routes intact.
location ^~ /.well-known/acme-challenge/ {
    root /var/lib/enterprise-exam-acme;
    try_files $uri =404;
}
